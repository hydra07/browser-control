---
name: typescript-skill
description: Mandatory TypeScript/JavaScript coding standard for high-performance, type-safe code - type inference discipline, satisfies, function and file structure, V8/JavaScriptCore engine rules for measured hot paths, and erasable-syntax constraints. Load before writing or reviewing any .ts/.tsx/.js/.jsx code, in any project.
---

Read `.agents/skills/typescript-skill/SKILL.md` at the repo root (from this
file's own location that's `../../../.agents/skills/typescript-skill/SKILL.md`)
and follow it, including the `references/RULES.md` it links to — that's the
single source of truth (also the path Antigravity CLI's own `.agents/skills/`
discovery reads natively), kept here only as a thin pointer instead of
duplicated so it can't drift. `@`-style inline transclusion does not expand
inside a SKILL.md body (only CLAUDE.md's own bootstrap gets that), so this
must be an explicit Read.
