node_modules/
dist/
data/
.claude/*
# Shared project skill/template, not a local setting — keep it tracked.
# (.claude/* not .claude/ — a directory-level ignore can't be un-ignored by
# a later "!" for anything inside it; globbing the children can.)
!.claude/skills/

test/
skills/*

# WXT (app/extension) — build output and its generated type-gen cache.
.output/
.wxt/
web-ext.config.ts

# Turborepo's local task cache.
.turbo/
