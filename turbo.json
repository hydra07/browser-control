{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "outputs": ["dist/**", ".output/**"]
        },
        "check": {
            "outputs": []
        }
    }
}
